# AI Fashion Shopping Assistant

Frontend-only React + Vite + Tailwind demo project for trainer presentations. Contains Home, Categories, Products and Product Details pages, dummy product data, and an AI Assistant demo modal.

To run:

```bash
npm install
npm run dev
```
